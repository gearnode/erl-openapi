%% File generated by erl-openapi on {{datetime}}.

-module({{package_name}}).

-export_type([info/0, contact/0, license/0,
              server/0]).

-export([info/0, servers/0]).

-type info() ::
          #{title := binary(),
            description => binary(),
            termsOfService => binary(),
            contact => contact(),
            license => license(),
            version := binary()}.

-type contact() ::
          #{name => binary(),
            url => binary(),
            email => binary()}.

-type license() ::
          #{name := binary(),
            url => binary()}.

-type server() ::
          #{url := binary(),
            description => binary(),
            variables =>
	        #{binary() := server_variable()}}.

-type server_variable() ::
          #{enum => [binary()],
            default := binary(),
            description => binary()}.

-spec info() -> info().
info() ->
{{info}}

-spec servers() -> [server()].
servers() ->
{{servers}}
